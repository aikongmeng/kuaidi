package gen.greendao.dao;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import gen.greendao.bean.UserBind;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "USER_BIND".
*/
public class UserBindDao extends AbstractDao<UserBind, Long> {

    public static final String TABLENAME = "USER_BIND";

    /**
     * Properties of entity UserBind.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property Master = new Property(1, String.class, "master", false, "MASTER");
        public final static Property Guest = new Property(2, String.class, "guest", false, "GUEST");
    }


    public UserBindDao(DaoConfig config) {
        super(config);
    }
    
    public UserBindDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"USER_BIND\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "\"MASTER\" TEXT," + // 1: master
                "\"GUEST\" TEXT);"); // 2: guest
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"USER_BIND\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, UserBind entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String master = entity.getMaster();
        if (master != null) {
            stmt.bindString(2, master);
        }
 
        String guest = entity.getGuest();
        if (guest != null) {
            stmt.bindString(3, guest);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, UserBind entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String master = entity.getMaster();
        if (master != null) {
            stmt.bindString(2, master);
        }
 
        String guest = entity.getGuest();
        if (guest != null) {
            stmt.bindString(3, guest);
        }
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public UserBind readEntity(Cursor cursor, int offset) {
        UserBind entity = new UserBind( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // master
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2) // guest
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, UserBind entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setMaster(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setGuest(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(UserBind entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(UserBind entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(UserBind entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
